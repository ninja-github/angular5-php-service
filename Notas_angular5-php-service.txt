Upload to gihub

Repository:
***********
Create repositorory on git hub

cd php-web-service-001
git init

Staging:
********
git add .
create .gitignore
git add .gitignore 
git commit -m "First Commit"
git branch

git remote add origin https://github.com/oa4ajp/php-web-service-001.git
git remote -v

git push -u origin master

Al hacer refresh en el server slae 404 not found. (--OK--)

Deploy:
*******
To reduce the deploy file size with optimization, reduce site load time.

ng build --prod --env=prod





	




